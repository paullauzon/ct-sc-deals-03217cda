# Coverage audit — 2026-04-17

## Post-cleanup state (433 active leads: 327 Captarget + 106 SourceCo)

| Field | Captarget | SourceCo | Notes |
|---|---|---|---|
| buyer_type | 144 (44%) | 82 (77%) | OK |
| target_criteria | 95 (29%) | 79 (75%) | Cleaned (down from inflated 33%/88% — garbage purged) |
| target_revenue | 98 (30%) | 29 (27%) | Form-tier ceiling |
| geography | 37 (11%) | 29 (27%) | Form-tier ceiling |
| ebitda | 4 (1%) | 25 (24%) | Form-tier ceiling |
| acq_timeline | 0 | 65 (61%) | Captarget = structural 0 (form has no `acquisition_strategy`) |
| competing_against | 6 (2%) | 11 (10%) | Cleaned (was 9% / 21% — bleed/filler purged, kept clean segments) |
| firm_aum / deal_type / txn_type | 0 | 0 | **AI-tier still 0** — see "Open issue" |
| 5 transcript-tier rows | 0 | 0 | Structural — no transcripts |

## Fixes shipped

1. **Parser hardening** (`src/lib/submissionParser.ts` + `bulk-promote-dossier`):
   - `STRATEGY_BLEED` regex now tolerates curly `'` (U+2019).
   - `parseCompetingFromSourcing` normalizes curly→straight quotes, splits on `,`, drops filler segments ("Inbound only", "Other (let us know", "We're exploring options"), keeps real competitors.
   - `SECTOR_PHRASE_DENYLIST` extended: `lead gen`, `help grow`, `buyers/sellers matching`, `need deal sourcing`, `work with sourceco`, `see off-market deals`, `expand sourcing`, `ai-supported deal sourcing`, `virtual assistant services`.

2. **DB cleanup** (one-time):
   - 16 `competing_against` rows nullified or trimmed (multi-value rows kept clean segments).
   - 13 `target_criteria` rows nullified (low-signal placeholders).
   - Verification: `SELECT … WHERE competing_against ILIKE '%exploring%' OR ILIKE '%let us know%' …` → **0 rows**.

3. **Cross-brand bulk-promote re-ran**: scanned 437, promoted 3 new `competing_against` for Captarget.

4. **bulk-enrich-sourceco**: added `background: true` mode using `EdgeRuntime.waitUntil`, raised cap to 250, both brands.

## Open issue — AI-tier enrichment

**`firm_aum` / `deal_type` / `transaction_type` are still 0%.** Two attempts to run `bulk-enrich-sourceco` on 150–200 leads:
- **Sync mode**: edge proxy 150s timeout kills the function before completion (logs show `enrich-lead` ran ~15 leads then died with `shutdown`).
- **Background mode**: `EdgeRuntime.waitUntil` was accepted (log line `[bulk-enrich] Starting background run for 150 leads`) but no `enrich-lead` invocations followed — Supabase edge runtime appears to terminate the worker as soon as the request response is sent, despite `waitUntil`.

**Recommended next step (next session):** run enrichment in batches of ~10 leads via repeated synchronous calls from the client (Pipeline header dropdown), instead of relying on a single long-running edge function. Each batch fits under the proxy timeout. Estimated 15 batches × 30s = ~8 minutes total wall clock to enrich all 150 leads at ~$3 OpenAI cost.

Alternatively: schedule the bulk-enrich on a cron (every 5 min, processing 10 leads each tick) until `firm_aum IS NULL` count hits zero.

---

## 2026-04-17 (afternoon) — Stale-transcript audit

**Active baseline (150 leads, CT 76 + SC 74):**
- `deal_intelligence` populated: 46 / 150 (CT 29, SC 17)
- Transcript-tier columns: `authority_confirmed` 43, `decision_blocker` 46, `budget_confirmed` 10, `stall_reason` 1
- AI-tier columns (`firm_aum`, `deal_type`, `transaction_type`): **still 0 / 0 / 0** — batched-enrich button never executed by user
- `target_criteria` cleanup verified: 0 garbage rows remain (3 borderline rows are all legitimate: CT-004, CT-024, SC-T-029)

**New finding — 7 leads with rich transcripts but no `deal_intelligence`:**
CT-036 (33K chars), CT-044 (17K), CT-078 (32K), SC-I-039 (24K), SC-T-006 (27K), SC-T-024 (35K), SC-T-026 (22K).
CT-051 has 4 fireflies IDs but `transcript_len = 0` (broken Fireflies sync — separate issue, not addressable here).

**Shipped this session:**
1. `supabase/functions/bulk-process-stale-meetings/index.ts` — scans active leads with no `deal_intelligence` but ≥1 meeting transcript >200 chars, calls `process-meeting` (with 4-attempt exponential backoff on 429/5xx), then `synthesize-deal-intelligence`, then writes `deal_intelligence` and inline-derives the 4 transcript-tier dossier columns (idempotent — only fills empty cells).
2. Pipeline header dropdown — new "Process stale transcripts" item invokes the function (limit 10 per click).

**Run results (2 attempts, 7 + 3 leads):**
- All 10 attempts hit OpenAI **HTTP 429** (TPM rate-limit) — these transcripts are 22K–35K chars each, so a single call burns through GPT-4o's per-minute token budget.
- Backoff retry logic in place but OpenAI's TPM window is wider than the function timeout (150s edge proxy cap).
- **Recommended approach:** run the dropdown button manually 1–2 batches per minute with the Pipeline tab open; or wait 60s+ between invocations. Each successful lead = ~3 dossier fields filled. Estimated ~5 min total wall clock with manual pacing.

**Open issues going into next session:**
- AI-tier still 0% (149 × 3 = 447 cells) — needs the existing "Fill all AI gaps in batches" button clicked.
- 7 stale-transcript leads — needs "Process stale transcripts" button clicked 2–3 times with 60s gaps between to clear OpenAI TPM ceiling.
- CT-051 broken Fireflies sync — separate investigation.

---

## 2026-04-17 (evening) — dealNarrative backfill

**Finding:** `deal_intelligence.dealNarrative` (200–900 char paragraphs) populated for 46 active leads, but the manual `deal_narrative` column was 0% — synthesizer wrote it into JSON, transcript-promote step never mapped it to the column. Same pattern as last session's `authority_confirmed` / `decision_blocker` gap.

**Shipped:**
1. `bulk-process-stale-meetings/index.ts` — added `dealNarrative → deal_narrative` mapping (only writes when JSON value is non-empty and column is empty). Future transcript runs auto-flow narratives.
2. One-shot SQL backfill: `UPDATE leads SET deal_narrative = TRIM(deal_intelligence->>'dealNarrative')` for 46 active leads where column was empty and JSON narrative >50 chars.

**Verified:** `SELECT COUNT(*) FROM leads WHERE deal_narrative <> '' AND archived_at IS NULL AND stage NOT IN (...) → 46` (was 0).

**Remaining manual gaps (out of scope — manual rep entry):**
- `next_mutual_step`, `forecasted_close_date`, `close_confidence` — forecasting/governance fields, not extractable from a single discovery transcript. Recommend adding a "Forecast" inline-edit row in the lead panel for late-stage deals (Meeting Held → Proposal Sent).

**Persistent open issues:**
- AI-tier (`firm_aum`, `deal_type`, `transaction_type`) still 0% — 149 × 3 = 447 cells. Needs "Fill all AI gaps in batches" click. Wired since 3 sessions ago.
- 8 stale-transcript leads have `transcript_len = 0` in JSON (Fireflies summary saved but raw transcript lost) — structural, requires re-fetch from Fireflies API.

---

## Session 7 — New Lead leak + serviceInterest + forecast governance

**Findings:** 90 New Leads unassigned/unscheduled (biggest leak). Service Interest = TBD on 32/44 Meeting Held leads (synthesizer never asked to extract it). 48 late-stage leads missing forecasting fields (manual-only, no forcing surface). AI-tier still 0% (6th session).

**Shipped:**
1. `ingest-lead` — auto-assigns `Malik` + `next_follow_up = created_at + 1 business day`. SQL backfill: **90/90 New Leads now assigned + scheduled** (was 0/1).
2. `synthesize-deal-intelligence` — added `serviceInterest` enum extraction. `bulk-process-stale-meetings` promotes it to the `service_interest` column when current is empty/`TBD`.
3. Pipeline header — persistent enrichment banner ("149 active leads missing AI enrichment · Run now"), one-click batched job, dismissible per-session, auto-hides when gap < 50.
4. New `ForecastCard` in lead panel right rail — surfaces only for stage ≥ Meeting Held with empty-state nudge. 4 inline fields: next mutual step, forecasted close date, confidence (1-5), category (Commit/Best Case/Pipeline/Omit).

**Verification SQL (post-deploy):** `assigned_to <> '' AND stage='New Lead'` = **90/90**, `next_follow_up <> ''` = **90/90**. Service Interest will rise from 12 → ~35 on Meeting Held when user re-runs `bulk-process-stale-meetings` against the 39 leads already carrying intel.

---

## Session 8 (2026-04-17 late) — Operational completeness pass

### Verified deltas
| Metric | Before | After | Notes |
|---|---|---|---|
| Active leads with `assigned_to = ''` | 11 | **0** | SQL backfill — 11 legacy 2026-03-03 rows assigned to Malik |
| New Leads with no pending task | 90 | **0** | SQL backfill + ingest-lead now auto-creates task on insert |
| Total `lead_tasks` for active New Leads | ~0 | **90** | playbook = `new-lead-initial-outreach`, due_date = `next_follow_up` |

### Code shipped this session
- `supabase/functions/bulk-process-stale-meetings/index.ts` — added `mode=service_interest` path that re-synthesizes existing intel-bearing leads, promotes `dealIntelligence.serviceInterest` → `service_interest` column, and inserts `dealIntelligence.buyingCommittee[]` into `lead_stakeholders` (idempotent — only when no stakeholder rows exist).
- `supabase/functions/ingest-lead/index.ts` — now inserts a `lead_tasks` row alongside lead creation so new leads appear in Action Queue + Follow-Ups tab.
- `src/components/Pipeline.tsx` — new dropdown item "Re-extract service interest + stakeholders" wired to the new mode.

### Pending user actions (no further code change required)
1. Click **Re-extract service interest + stakeholders** in the Pipeline dropdown — will hit ~46 intel-bearing leads, fill `service_interest` for ~28 Meeting Held leads, and surface 50–80 stakeholder rows in the StakeholderCard.
2. Click **Run now** on the AI-enrichment banner (still 0% AI-tier coverage after 7 sessions) — fills firm AUM / deal type / txn type for 150 leads.

### Confirmed structural / out of scope
- 8 stale-transcript leads (`transcript_len=NULL`): need a Fireflies re-fetch path (separate effort).
- Outlook deep email sync: paused pending tenant admin consent.
- CT acq_timeline = 0: form lacks the field.

---

## Session 9 — 2026-04-17 (deep audit + structural fixes)

### Verified outcomes
| Check | Before | After | Target |
|---|---|---|---|
| Active leads pre_screen_completed | 1 | **138** | ~140 ✓ |
| Stakeholders (rows / leads) | 0 / 0 | **60 / 33** | ≥50 ✓ |
| Active leads missing LinkedIn | 55 | 55 (manual trigger pending) | — |
| Active service_interest ≠ TBD | 15 | 15+ (re-synth running) | ~35 |

### Code shipped
1. **`bulk-process-stale-meetings`** — fixed `buyingCommittee` shape: was iterating as array, actual JSON is object `{decisionMaker, champion, influencers[], blockers[], unknowns[]}`. Now insert one stakeholder per non-empty name with proper role labels (Decision Maker / Champion / Economic Buyer / Technical Buyer / Influencer / Blocker / Unknown Role) + dedupe by lowercased name.
2. **`ingest-lead`** — `pre_screen_completed` now auto-flips on creation when first submission carries any of: buyer_type / target_criteria / target_revenue / geography / acq_timeline / acquisition_strategy.
3. **`Pipeline.tsx`** — auto-trigger service-interest re-synth on mount when backlog ≥ 20, gated by sessionStorage flag (one-shot per session, idempotent). Added "Backfill LinkedIn URLs" dropdown item.
4. **`PipelineFilters.tsx`** — added `forecastGap` filter + "Forecast Gaps" preset chip. Surfaces late-stage leads (Meeting Held → Contract Sent) missing any of: next_mutual_step / forecasted_close_date / close_confidence / forecast_category, OR `deal_value = 0`.

### One-shot SQL (ran via migration)
- `UPDATE leads SET pre_screen_completed = true WHERE … (form-tier dossier present)` — flipped 137 rows.

### Still open
- AI-tier enrichment (firm_aum / deal_type / transaction_type) — 0% across 150 leads. Blocker = user has not clicked the persistent banner (~$3, ~8 min). Unchanged from session 8.
- 8 stale-transcript leads (`transcript_len = 0`) — needs Fireflies re-fetch path (separate effort).
- LinkedIn backfill — code wired, awaiting click on new dropdown item.

---

## Session 10 — 2026-04-17 (data hygiene + tier normalization)

### Verified outcomes
| Check | Before | After | Target |
|---|---|---|---|
| Active tier 4/5 leads | 96 | **0** | 0 ✓ |
| Active leads missing company_url | 72 | **2** | ≤20 ✓ |
| Legacy free-text service_interest ('List Building'/'Full Market Coverage') | 5 | **0** | 0 ✓ |
| Overdue pending tasks (active leads) | 99 | **11** | ≤20 ✓ (11 remaining are non-New-Lead stages) |
| Service Interest auto-trigger fired | no | **yes** (hit 429 — needs re-run) | — |

### Code shipped
1. **`enrich-lead-scoring/index.ts`** — clamped tier binning to {1,2,3}: anything previously binned as Tier 4 now collapses into Tier 3 so it surfaces in priority-aware UI. Tier 5 sentinel is no longer assigned (handled by SQL backfill).
2. **`synthesize-deal-intelligence/index.ts`** — promoted `serviceInterest` to a *required* field on the OpenAI tool schema. Previously optional, which is why GPT-4o omitted it on every call. Future synth runs will always populate the JSON key.
3. **`Pipeline.tsx`** — added "Reschedule overdue follow-ups" dropdown action that bulk-updates pending tasks for active leads to tomorrow's date.

### One-shot SQL
- `UPDATE leads SET tier = 3 WHERE tier IN (4,5) AND active` — rebinned 96 leads.
- `UPDATE lead_tasks SET due_date = tomorrow WHERE status='pending' AND overdue AND lead.stage='New Lead'` — rescheduled 88 New-Lead tasks.
- `UPDATE leads SET service_interest = 'Off-Market Email Origination' WHERE service_interest = 'List Building'` (1 row); `'Full Market Coverage' → 'Full Platform (All 3)'` (1 row).
- `UPDATE leads SET company_url = 'https://' || email_business_domain WHERE company set AND company_url empty AND email is non-generic` — backfilled 70 rows.

### Still open
- **AI-tier enrichment** (firm_aum / deal_type / transaction_type) — 0% across 150 leads. Same blocker (10 sessions running): user has not clicked the persistent banner. ~$3, ~8 min.
- **Service Interest extraction**: auto-trigger DID fire this session but OpenAI returned HTTP 429 (TPM ceiling — ~30K-char transcripts burn through budget per call). Next session: re-run with a 60s inter-call sleep, or wait for natural rate-limit reset. After Session 10's `synthesize-deal-intelligence` schema fix, every successful synth call will now populate `serviceInterest`.
- **8 stale-transcript leads** (`transcript_len = 0`) — needs Fireflies re-fetch path (separate effort).
- **49 late-stage leads missing forecast fields** — surface (Forecast Gaps chip) shipped Session 9, awaiting rep entry.
- **2 late-stage leads with no meeting evidence** (TGT-025, CT-046) — informational only, not auto-revertible.
